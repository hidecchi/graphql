{
  "name": "graphql",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npm install &&  npm install --prefix server && node ./server/app.js",
    "tt":"cd .. "
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "16.13.2",
    "npm": "7.22.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
